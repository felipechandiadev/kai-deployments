# Cliente: demo

Ambiente público/demo (`*.demo.kaisuite.pro`).

- Matriz de puertos de referencia: `ports.env.example` (origen: `kai-suite/deploy/ports.demo.env.example`).
- Código: clone/pull de `kai-suite` en el VPS; este folder solo orquesta.
